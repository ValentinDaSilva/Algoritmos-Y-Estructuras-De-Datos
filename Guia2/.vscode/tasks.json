{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compilarYejecutar",
            "type": "shell",
            "command": "sh",
            "args": [
                "-c",
                "g++ ${file} -o ${fileDirname}/${fileBasenameNoExtension} && ${fileDirname}/${fileBasenameNoExtension} && sleep 3s &&rm ${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}